on: push
jobs:
  job1:
    runs-on: windows-latest
    steps:
      - name: Install Poetry
        uses: snok/install-poetry@v1
      - name: Poetry run
        run: poetry run mkdocs build --config-file mkdocs.yml
